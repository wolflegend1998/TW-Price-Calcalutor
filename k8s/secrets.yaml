# kubectl create secret generic tw-secrets \
#   --from-literal=anthropic-api-key=YOUR_KEY \
#   -n tw-calculator
# Do NOT commit real keys. Use this as reference only.
apiVersion: v1
kind: Secret
metadata:
  name: tw-secrets
  namespace: tw-calculator
type: Opaque
stringData:
  anthropic-api-key: "REPLACE_ME"
